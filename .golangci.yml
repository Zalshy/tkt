linters:
  enable:
    - errcheck
    - staticcheck
    - govet
    - unused
    - revive
    - gocritic

linters-settings:
  revive:
    rules:
      - name: exported
        disabled: true

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
